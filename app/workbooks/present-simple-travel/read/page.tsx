import PresentSimpleWorkbook from "../PresentSimpleWorkbook";import {getChatGPTUser} from "../../../chatgpt-auth";import "../workbook.css";export const dynamic="force-dynamic";export default async function Page({searchParams}:{searchParams:Promise<{teacher?:string}>}){const user=await getChatGPTUser(),teacherPreview=(await searchParams).teacher==="1"&&user?.email.toLowerCase()==="librimaniastore@gmail.com";return <PresentSimpleWorkbook teacherPreview={teacherPreview}/>}
